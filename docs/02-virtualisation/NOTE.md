# Note — Étape 02

L'environnement de virtualisation est prêt. Les cinq machines virtuelles sont éteintes et aucun système d'exploitation n'a encore été installé.

La prochaine intervention concernera `DC-CASA` : installation de Windows Server, attribution de l'adresse IP `192.168.10.10` et préparation du premier contrôleur de domaine.
